[build-system]

requires = [
    "setuptools>=63",
]

build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100

[tool.pylint.messages_control]

max-line-length = 100

disable = [
  "invalid-name",
  "too-few-public-methods"
]
